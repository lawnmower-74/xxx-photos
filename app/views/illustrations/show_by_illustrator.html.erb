<%# --- ギャラリー化ライブラリ --- %>
<script src="https://cdn.jsdelivr.net/npm/spotlight.js@0.7.8/dist/spotlight.bundle.js"></script>

<div style="max-width: 1000px; margin: 0 auto; padding: 20px;">
  <div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <%= link_to "< フォルダ一覧に戻る", root_path, style: "color: #666; text-decoration: none;" %>
    
    <%# --- 一括削除ボタン --- %>
    <button id="bulk-delete-btn" style="display: none; padding: 8px 16px; background: #dc3545; color: white; border: none; border-radius: 4px; cursor: pointer;">
      選択した項目を削除
    </button>
  </div>

  <h1><%= @illustrator.name %> の作品一覧</h1>

  <%# --- 並べ替えボタン --- %>
  <div style="margin-bottom: 20px; display: flex; align-items: center; gap: 10px; background: #f8f9fa; padding: 15px; border-radius: 8px;">
    <span style="font-weight: bold; font-size: 0.9em;">並べ替え：</span>

    <%= link_to "最新順", illustrator_folder_path(name: @illustrator.name, sort: 'desc'), 
        class: "sort-link #{'active' if params[:sort] != 'asc'}" %>

    <span style="color: #ccc;">|</span>

    <%= link_to "古い順", illustrator_folder_path(name: @illustrator.name, sort: 'asc'), 
        class: "sort-link #{'active' if params[:sort] == 'asc'}" %>
  </div>

  <%# --- 類似画像一覧 --- %>
  <% if @similar_illustrations.present? %>
    <details style="margin-bottom: 40px; padding: 10px; background: #fff9f0; border: 2px dashed #ffc107; border-radius: 12px;">
      <summary style="padding: 5px 20px; cursor: pointer; list-style: none; display: flex; justify-content: space-between; align-items: center; user-select: none;">
        <h2 style="margin: 0; color: #856404; font-size: 1.0em; display: flex; align-items: center; gap: 8px;">
          ⚠️ もしかしたら同じ画像かも？ (<%= @similar_illustrations.size %>枚)
        </h2>
        <span class="tgl-icon" style="transition: transform 0.3s; color: #856404; font-weight: bold;">▼</span>
      </summary>

      <div style="padding: 0 20px 20px 20px;">
        <p style="font-size: 0.85em; color: #7d6a3a; margin-bottom: 15px;">
          特徴が非常に近い画像が見つかりました。不要なものを削除して整理できます。
        </p>
        
        <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 15px;">
          <% @similar_illustrations.each do |illustration| %>
            <div class="illustration-item" id="similar-item-<%= illustration.id %>" style="position: relative; border: 1px solid #ffeeba; border-radius: 8px; overflow: hidden; background: white;">

              <%# 一括削除用チェックボックス %>
              <input type="checkbox" class="delete-checkbox" data-id="<%= illustration.id %>" 
                    style="position: absolute; top: 8px; left: 8px; z-index: 10; width: 18px; height: 18px; cursor: pointer;">
              
              <%# 画像表示 %>
              <% if illustration.image.attached? %>
                <%# 拡大リンク（クリック時に原寸大の画像を表示） %>
                <a href="<%= url_for(illustration.image) %>" 
                  class="spotlight" 
                  data-group="similar-gallery"
                  style="display: block;">
                  <%# サムネイル %>
                  <%= image_tag illustration.image.variant(resize_to_limit: [200, 200]),
                                loading: "lazy", 
                                style: "width: 100%; height: 140px; object-fit: cover; display: block;" %>
                </a>
              <% end %>

              <div style="padding: 6px; text-align: center;">
                <%# 撮影日時 %>
                <span style="font-size: 0.75em; color: #666;">
                  📅 <%= illustration.shot_at&.strftime("%m/%d %H:%M") %>
                </span>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </details>
  <% end %>

  <%# --- 画像一覧 --- %>
  <div id="illustration-list" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px;">
    <% @illustrations.each do |illustration| %>
      <div class="illustration-item" id="item-<%= illustration.id %>" style="position: relative; border: 1px solid #ddd; border-radius: 8px; overflow: hidden; background: white; transition: all 0.3s ease;">
        
        <%# 一括削除用チェックボックス %>
        <input type="checkbox" class="delete-checkbox" data-id="<%= illustration.id %>" 
               style="position: absolute; top: 10px; left: 10px; z-index: 10; width: 20px; height: 20px; cursor: pointer;">

        <%# 画像表示 %>
        <% if illustration.image.attached? %>
          <%# 拡大リンク（クリック時に原寸大の画像を表示） %>
          <a href="<%= url_for(illustration.image) %>"
             class="spotlight" 
             data-group="gallery"
             style="display: block;">
            <%# サムネイル %>
            <%= image_tag illustration.image.variant(resize_to_limit: [300, 300]), 
                          loading: "lazy",
                          style: "width: 100%; height: 200px; object-fit: cover; display: block;" %>
          </a>
        <% end %>

        <div style="padding: 10px; display: flex; justify-content: space-between; align-items: center;">
          <%# 撮影日時 %>
          <span style="font-size: 0.8em; color: #666;">
            📅 <%= illustration.shot_at&.strftime("%m/%d %H:%M") %>
          </span>

          <div style="display: flex; gap: 10px;">
            <%# アルバムカバー設定ボタン %>
            <button class="set-cover-btn" data-id="<%= illustration.id %>" 
                    style="background: none; border: none; color: #007bff; cursor: pointer; font-size: 0.9em;"
                    title="この画像をフォルダの表紙にする">
              ★ 表紙
            </button>

            <%# 個別削除ボタン %>
            <button class="single-delete-btn" data-id="<%= illustration.id %>" 
                    style="background: none; border: none; color: #dc3545; cursor: pointer; font-size: 0.9em;">
              削除
            </button>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<style>
  /* ================================================ */
  /* 一括削除（選択時）に関するスタイル */
  /* ================================================ */
  .illustration-item.selecting {
    border-color: #007bff !important;
    box-shadow: 0 0 10px rgba(0,123,255,0.2);
  }
  /* 画像の拡大リンク（Spotlight）を無効化するクラス */
  .disable-gallery {
    pointer-events: none;
    cursor: default;
  }

  /* ================================================ */
  /* 類似画像のアコーディオン形式に関するスタイル */
  /* ================================================ */
  /* ブラウザ標準のトグルボタンを非表示化 */
  summary::-webkit-details-marker {
    display: none;
  }

  /* トグルボタンのアニメーション */
  details[open] .tgl-icon {
    transform: rotate(180deg);
  }

  /* ホバー時の背景色変更 */
  summary:hover {
    background-color: rgba(255, 193, 7, 0.1);
  }
</style>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const bulkDeleteBtn = document.getElementById("bulk-delete-btn");
  const csrfToken = document.querySelector('meta[name="csrf-token"]').content;

  // ===================================
  // 画像削除処理
  // ===================================
  // -----------------------------------
  // 選択モード切り替え（一括削除）
  // -----------------------------------
  const toggleSelectingMode = () => {
    const checkboxes = document.querySelectorAll(".delete-checkbox");
    const checkedCount = Array.from(checkboxes).filter(cb => cb.checked).length;
    const isSelecting = checkedCount > 0;

    // 拡大リンクの有効／無効 切り替え（1枚でも選択された場合は拡大を無効化）
    document.querySelectorAll(".illustration-item").forEach(item => {
      const link = item.querySelector(".spotlight");
      item.classList.toggle("selecting", isSelecting);
      link.classList.toggle("disable-gallery", isSelecting);
    });

    // 一括削除ボタンの表示／非表示の切り替え
    if (bulkDeleteBtn) {
      bulkDeleteBtn.style.display = isSelecting ? "block" : "none";
      setBulkDeleteBtnState("idle", checkedCount)
    }
  };

  // 「一括削除ボタン」表示時 のUIを管理
  const setBulkDeleteBtnState = (state, checkedCount = 0) => {
    if (!bulkDeleteBtn) return;

    switch (state) {
      case "idle": // 通常（待機）状態
        bulkDeleteBtn.disabled = false;
        bulkDeleteBtn.innerText = `選択した ${checkedCount} 件を削除`;
        bulkDeleteBtn.style.backgroundColor = "#dc3545";
        bulkDeleteBtn.style.cursor = "pointer";
        break;

      case "loading": // 削除実行中
        bulkDeleteBtn.disabled = true;
        bulkDeleteBtn.innerText = "削除処理中...";
        bulkDeleteBtn.style.backgroundColor = "#6c757d";
        bulkDeleteBtn.style.cursor = "not-allowed";
        break;
    }
  }

  // -----------------------------------
  // 削除処理（個別／一括共通）
  // -----------------------------------
  const deleteIllustrations = async (ids) => {
    // 確認用ダイアログ表示
    const message = ids.length > 1 
      ? `選択した ${ids.length} 件の画像を完全に削除してもよろしいですか？` 
      : "この画像を削除してもよろしいですか？";

    if (!window.confirm(message)) return;

    // 「一括削除ボタン」の状態を「削除中」に切り替え
    setBulkDeleteBtnState("loading")

    try {
      const response = await fetch("/illustrations/bulk_destroy", {
        method: "DELETE",
        headers: { 
          "X-CSRF-Token": csrfToken,
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ ids: ids })
      });

      if (response.ok) {
        // 画像を画面上からも消す
        ids.forEach(id => {
          // IDが一致する要素をすべて取得（一覧・類似セクションの両方）
          const items = [
            document.getElementById(`item-${id}`),
            document.getElementById(`similar-item-${id}`)
          ].filter(el => el !== null);

          items.forEach(element => {
            element.style.transition = "all 0.3s ease";
            element.style.opacity = "0";
            element.style.transform = "scale(0.8)";
            setTimeout(() => {
              element.remove();
              
              // 類似セクションが空になったらセクションごと消す
              const similarContainer = document.querySelector('[style*="background: #fff9f0"]');
              if (similarContainer) {
                const remainingItems = similarContainer.querySelectorAll('.illustration-item');
                if (remainingItems.length === 0) {
                  similarContainer.remove();
                }
              }
              
              toggleSelectingMode(); // 選択モードOFF
            }, 300);
          });
        });
      } else {
        const errorData = await response.json().catch(() => ({}));
        alert("削除に失敗しました: " + (errorData.error || "サーバーエラー"));
        toggleSelectingMode(); // 選択モードON（※チェックしたものが残っていた場合）
      }
    } catch (err) {
      console.error("JS関連のエラー: ", err);
      alert("エラーが発生しました。");
      toggleSelectingMode(); // 選択モードON（※チェックしたものが残っていた場合）
    }
  };

  // ===================================
  // アルバムカバー設定処理
  // ===================================
  const setFolderCover = async (imageId) => {
    const folderName = "<%= @illustrator.name %>";

    try {
      const response = await fetch(`/folders/${encodeURIComponent(folderName)}/set_cover`, {
        method: "PATCH",
        headers: { 
          "X-CSRF-Token": csrfToken,
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ image_id: imageId })
      });

      if (response.ok) {
        alert("アルバムの表紙を更新しました！");
        window.location.reload(); 
      } else {
        alert("設定に失敗しました。");
      }
    } catch (err) {
      console.error("カバー設定エラー: ", err);
      alert("通信エラーが発生しました。");
    }
  };

  // ===================================
  // イベントリスナーの設定
  // ===================================
  // クリックイベント
  document.addEventListener("click", (e) => {
    const deleteBtn = e.target.closest(".single-delete-btn");
    const coverBtn = e.target.closest(".set-cover-btn");
    const bulkBtn = e.target.closest("#bulk-delete-btn");

    // 削除ボタン
    if (deleteBtn) {
      e.preventDefault(); // ※リンク（拡大表示）の発火を防ぐ
      deleteIllustrations([deleteBtn.dataset.id]);
      return;
    }

    // アルバムカバー設定ボタン
    if (coverBtn) {
      e.preventDefault();
      setFolderCover(coverBtn.dataset.id);
      return;
    }

    // 一括削除ボタン
    if (bulkBtn) {
      const selectedIds = Array.from(document.querySelectorAll(".delete-checkbox:checked")).map(cb => cb.dataset.id);
      deleteIllustrations(selectedIds);
      return;
    }

    // 選択モードONであれば、画像のどこをクリックしてもチェック反転しかできないようにする
    const item = e.target.closest(".illustration-item");
    if (item) {
      const checkbox = item.querySelector(".delete-checkbox");

      // 画面全体でチェックされたものがあるか（選択モードONか）を確認
      const isSelecting = document.querySelectorAll(".delete-checkbox:checked").length > 0;
      if (isSelecting) {
        // チェックボックス以外の場所（※画像カード枠内）をクリックしたなら、チェックを反転させる
        if (e.target !== checkbox) {
          e.preventDefault();
          checkbox.checked = !checkbox.checked;
          toggleSelectingMode(); // 選択モードON／OFF
        }
      }
    }
  });

  // チェックボックス（チェック入れた時／外した時）
  document.addEventListener("change", (e) => {
    if (e.target.classList.contains("delete-checkbox")) {
      toggleSelectingMode(); // 選択モードON／OFF
    }
  });
});
</script>